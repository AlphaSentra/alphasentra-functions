services:
  - type: web
    name: ago-functions
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 --timeout 3600 -b 0.0.0.0:$PORT app:app
    healthCheckPath: /
