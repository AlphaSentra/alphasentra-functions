services:
  - type: web
    name: ago-functions
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 --max-requests 500 --timeout 120 -b 0.0.0.0:$PORT app:app
    healthCheckPath: /
